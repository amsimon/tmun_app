.column.full
  .module
    .title
      %span
        SPEAKERS
    .admin-container.add
      = link_to 'javascript:;' do
        .clicker.add A
      %ul.admin-list
        %li
          = link_to new_cconference_speaker_path(@cconference), class: "admin-link" do
            .admin-line
              .icon
                = image_tag 'admin-icons/create.png'
              .words
                Add speaker
    - if @cconference.speakers.size == 0
      Speakers will be announced at a later date.
    - else
      %div.conf_cont
        .speaker-cont
          = render partial: "speakers/speaker", collection: @cconference.speakers, as: :speaker, locals: {parent: @cconference}
